# Changelog

This plugin tracks the `@composer-app/mcp` npm package. Each release of
the MCP package bumps this plugin to the same version and republishes
the synced contents.

## 0.0.1

Synced against `@composer-app/mcp@0.0.1`. See the upstream repo's
release notes for the full changelog.
